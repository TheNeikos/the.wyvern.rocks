.row
  .columns.small-offset-1.small-10
    = simple_form_for @user, url: session_path do |f|
      = f.error :base
      = f.input :email
      = f.input :password
      = f.button :submit, "Sign in"
